quarkus.application.name=badass-model
quarkus.http.port=12360

# Enable CORS for OpenCode client
quarkus.http.cors=true
quarkus.http.cors.origins=*
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.xenoamess.badass_model".level=DEBUG

# Native image hints
quarkus.native.additional-build-args=-H:+ReportExceptionStackTraces
